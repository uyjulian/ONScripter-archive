# -*- Makefile -*-
#
# Makefile.Linux - Makefile rules for linux
#

INCS = `sdl-config --cflags`
LIBS = `sdl-config --libs` -lSDL_ttf -lSDL_image -lSDL_sound -lSDL_mixer -lbz2

EXESUFFIX =
OBJSUFFIX = .o

.SUFFIXES:
.SUFFIXES: $(OBJSUFFIX) .cpp .h

CC = g++ 
LD = g++ -o 

CFLAGS = -g -DLINUX -Wall -Wpointer-arith -fcheck-new -pipe -c $(INCS)
#CFLAGS = -O3 -DLINUX -Wall -Wpointer-arith -fcheck-new -pipe -c $(INCS)
RM = rm -f

TARGET = onscripter$(EXESUFFIX) sardec$(EXESUFFIX) nsadec$(EXESUFFIX)

include Makefile.onscripter
